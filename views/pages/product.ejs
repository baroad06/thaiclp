<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="/css/contactus.css">
    <link rel="stylesheet" href="/css/essential.css">
    <%- include ../partials/includings.ejs %>
    <title>ผลิตภัณฑ์ของไทยชลธารพลาส</title>
</head>
<body>
    <%- include ../partials/navbar.ejs %>
    <%- include('../partials/cover.ejs', {link: '/images/product/cover.jpg'}) %>
    <div class="container">
        <div class="row">
            <div class="col-md"></div>
            <div class="col-md-9">
                <h1 style="text-align: center">
                    ผลิตภัณฑ์ของไทยชลธารพลาส (Catalogue)
                </h1>
                <% var subfolder = product.subfolder %>
                <% for (var i = 0; i < subfolder.length/2; i++) { %>
                    <div class="row">
                        <div class="col-md">
                            <a href="/product/<%= subfolder[2*i].url %>"><img class="img-responsive img-article-bottom" src="/images/catalog/<%= subfolder[i*2].url %>/cover.jpg"></a>
                            <a href="/product/<%= subfolder[2*i].url %>"><div style="text-align: center"><%= subfolder[2*i]["name"] %></a><br></div>
                        </div>
                        <div class="col-md">
                            <% if ((2*i)+1 < subfolder.length) { %>
                            <a href="/product/<%= subfolder[2*i+1].url %>"><img class="img-responsive img-article-bottom" src="/images/catalog/<%= subfolder[i*2+1].url %>/cover.jpg"></a>
                            <a href="/product/<%= subfolder[2*i+1].url %>"><div style="text-align: center"><%= subfolder[2*i+1]["name"] %></a><br></div>
                            <% } %>
                        </div>
                    </div>
                <% } %>
            </div>
            <div class="col-md"></div>
        </div>
    </div>
    <%- include ../partials/footer.ejs %>
</body>
</html>
